# Validation Log

Results for `doc/validate_board_plan.md`.

| Date | Stage | Step | What I did | Result (pass/fail) | Notes, values, photos |
|---|---|---|---|---|---|
| 2026-09-19 | 0 | 3 | `idf.py --version` | pass | ESP-IDF v4.4.4. Harmless warnings: `pkg_resources` deprecated (setuptools<81 workaround) and Click `MultiCommand` deprecated. |
| 2026-09-19 | 0 | 1 | Photos | skipped | Skipped by choice. |
| 2026-09-19 | 1 | 1 | Found port | pass | `/dev/cu.usbserial-0001` (use `cu.*`, not `tty.*`). |
| 2026-09-19 | 1 | 2 | Measured 3V3 to GND | pass | 3.374 V (expect about 3.38 V). |
| 2026-09-19 | 1 | 3 | `esptool.py chip_id` (v3.3.2) | pass | ESP32-D0WD-V3, revision 3 (meets `CONFIG_ESP32_REV_MIN_3`). WiFi, BT, dual core, 240 MHz, 40 MHz crystal, MAC 70:4b:ca:24:f8:54, coding scheme None. First attempt failed: QGroundControl had the port open (quit it). |
| 2026-09-19 | 1 | 3 | `esptool.py flash_id` | pass | Manufacturer 0x5e (Zbit), device 0x4016, 4 MB (confirms 4 MB, not the main firmware's 16 MB). |
| 2026-09-19 | 1 | 5 | `espefuse.py summary` (read only) | pass | Factory state, nothing burned. XPD_SDIO_FORCE=False, so flash voltage is set by GPIO12 at reset (high = 1.8 V, low/floating = 3.3 V). Flash is a 3.3 V part, so GPIO12 must be low at boot: watch this with the SD card in (stage 8 step 3). JTAG_DISABLE=False, UART_DOWNLOAD_DIS=False, no flash encryption, no secure boot, WR_DIS=0, ADC_VREF=1128 mV, MAC CRC OK. |
| 2026-09-19 | 1 | 4 | Auto-reset | pass | Connected on its own, no manual BOOT/RESET needed. |
| 2026-09-19 | 1 | 6 | `chip_id` at 115200 and 921600 | pass | Both connect and switch baud cleanly. `chip_id` moves little data, so re-check 921600 with a real flash in stage 2. Stage 1 complete. |
| 2026-09-19 | 2 | 1-3 | Created `hwtest/`, built with `idf.py build` | pass (build only) | 4 MB, DIO 40 MHz, UART0 115200, min rev 3, PSRAM off. App 0x298e0 bytes. Not flashed yet. |
| 2026-09-19 | 2 | 3 | `idf.py -p /dev/cu.usbserial-0001 -b 921600 flash monitor` | pass | 170 KB app written in 1.5 s at 921600, hashes verified, so 921600 is reliable for flashing. |
| 2026-09-19 | 2 | 4 | Boot log and hello lines | pass | Bootloader: chip revision v3.1, DIO 40 MHz, 4 MB, partition table OK. App: 2 cores, WiFi/BT classic/BLE, free heap 299132 bytes, CPU 160 MHz (IDF default, not 240). `spi_flash: detected chip: generic` (Zbit uses the generic driver). Reset reason after the flash reset: power-on. One line per second, uptime steps of exactly 1000 ms. |
| 2026-09-19 | 2 | 5 | RESET button | pass | Counter restarted, as expected (reported by user; reset reason text not recorded). |
| 2026-09-19 | 2 | 6 | 100 lines/s console stress | pass | Sample pasted: lines 1158-1211, consecutive, uptime steps of exactly 10 ms, and line 1211 at 12154 ms (no drift, so no drops up to 12 s). Full minute reported "seems ok" but only about 12 s was pasted. Stage 2 complete. |
| 2026-09-19 | 3 | 1-2 | Flashed stage 3 `hwtest`, PSRAM check | pass | PSRAM: 64 Mbit device found, chip size 8 MB, heap 4194303 bytes (4 MB mapping limit; himem needed for the rest), 1 MB write/read test 0 mismatches (write 6.7 MB/s, read 10.0 MB/s at 40 MHz PSRAM). Boot memtest adds about 1 s to boot. Free heap with PSRAM 4490915 bytes. |
| 2026-09-19 | 3 | 3 | I2C scan, DIP all OFF | pass | **Pair B, SDA 33 / SCL 32: ES8388 at 0x10. Pair A, SDA 18 / SCL 23: no devices.** So this is the OLDER ES8388 module; GPIO 18, 23, 5 are free (after removing R68-R70). |
| 2026-09-19 | 3 | 4 | Updated `audio_kit_v2.2.md` sections 1, 4, 6, 9 and the plan's stage 4 | done | BCLK expected on IO27 (verify in stage 6). |
| 2026-09-19 | 4 | 1 | Resistance to GND, board unpowered, DIP all OFF except SW4 ON | recorded | IO22 35 MΩ, IO19 40 MΩ, IO18 46 MΩ, IO23 46 MΩ, IO5 42 MΩ (all effectively open); IO21 110 kΩ (R46 + R51 pull-down to the amp enable); MTCK/IO13 **10 kΩ**; MTDO/IO15 OL (open); MTMS/IO14 13 kΩ; MTDI/IO12 350 kΩ. MTCK reading matches a 10 kΩ pull-down (R15?); MTMS 13 kΩ unexplained (SD CLK, not in our plan). |
| 2026-09-19 | 4 | decision | Hook input polarity | decided | IO13 / MTCK is active-high: hook contact between 3V3 and MTCK, R15 (10 kΩ) left as the pull-down. Firmware handles the polarity; the main firmware (`pots_task`) is changed later with the planned hook/dial rewrite, not before the board is proven. Docs updated. |
| 2026-09-19 | 4 | 1 | MTCK to GND with SW4 OFF | recorded | Still 10 kΩ, so the pull-down is on the header side of the DIP switch (consistent with R15, which has no printed label on the board). Works the same with SW4 ON. |
| 2026-09-19 | 4 | 2 | Idle voltages to GND, USB powered, stage 3 firmware running | recorded, needs follow-up | MTCK/IO13 0 V; MTDO/IO15 0.7 V; MTMS/IO14 3.37 V; IO22 3.1 V; IO19 1.97 V; **IO18 4.01 V and IO23 4.15 V (double checked)**. The 3V3 rail measured 3.374 V, so IO18 and IO23 are above VDD (about 0.65-0.8 V over, which looks like the pin's protection diode clamping while a higher voltage feeds it through a high resistance). Source not identified. Nothing to be connected to IO18/IO23 until explained. |
| 2026-09-19 | 4 | 2 | More idle voltages, and resistance to the 3V3 pin (USB unplugged) | recorded, unexplained | 3V3 pin 3.375 V; **IO5 4.04 V** (same as IO18 4.01 V, IO23 4.15 V); IO21 0 V. Resistance to the 3V3 pin: IO18 46 MΩ, IO23 46 MΩ, IO19 OL. So the three key-ladder pins (IO5, IO18, IO23) sit above VDD when powered, yet show no DC path to 3V3 or GND when unpowered. Source still unidentified; it must be active only when powered. |
| 2026-09-19 | 4 | 4 | Key coupling by hand, USB powered | pass | Each key (KEY2 to IO13, KEY3 to IO19, KEY4 to IO23, KEY5 to IO18, KEY6 to IO5) pulled only its own pin to 0 V; the keys are decoupled from each other. After the first press all three high pins settled at 3.36 V and the earlier 4 V+ could not be reproduced, even after disconnecting everything and power cycling. Likely cause: the high-resistance ohmmeter readings earlier had charged the floating pins above VDD (clamped at about VDD + 0.65 V, which matches 4.01-4.15 V), and nothing discharged them until a key press. Not proven. |
| 2026-09-19 | 4 | decision | R67-R69 removal | deferred | The ladder coupling looks absent, so removal is probably unnecessary. Decide after the firmware input test (pins read with internal pulls off). |
| 2026-09-19 | 4 | note | KEY3 | noted | KEY3 (IO19) is physically broken open (moving parts missing). IO19 is tested with a jumper wire to GND instead. |
| 2026-09-19 | 4 | 3 | Wrote the stage 4 console firmware in `hwtest/` (commands `pins`, `mode`, `set`, `watch`, `boot`; stage 2 and 3 tests are now the commands `hello`, `psram`, `i2cscan`, `info`) | built | Clean build, not yet flashed. Instructions in `hwtest/README.md`. |
| 2026-09-19 | 4 | 3 | First `watch on` with all pins as plain inputs | finding | IO5, IO18 and IO23 toggled constantly, in step with 60 Hz (edges about 16.7 ms apart): they float with **no pull-up on the board** and pick up mains hum. IO13/MTCK (R15 pull-down), IO19 and IO22 did not flood. So the key ladder gives no pull-up: dial inputs need a pull-up (external 4.7-10 kΩ, or the internal one). `watch` now switches a pin off after 200 edges/s. |
| 2026-09-19 | 4 | 3 | `boot` and `pins`, nothing touched | recorded | Boot levels: IO13/MTCK 0, IO18 0, IO23 0, IO22 1, IO19 1, IO5 1, IO21 0. Floating pins (IO18, IO23, IO5) read differently at boot and later (IO18/IO23 0 then 1, IO5 1 then 0): noise, not a real level. IO22 and IO19 idle high. |
| 2026-09-19 | 4 | 4 | Internal pull-ups on IO5, IO18, IO23, IO19, then `watch` while pressing KEY4, KEY5, KEY6 | pass | All four read 1 with the pull-up. Each key printed `-> 0` on press and `-> 1` on release (about 0.15-0.2 s down), and **only its own pin changed: keys decoupled**. Bounce is small: on IO5, extra edges 166 us and 64 us before the final release; none seen on IO18 or IO23. 0 edges dropped. |
| 2026-09-19 | 4 | 4 | Hook jumper 3V3 to MTCK | pass (partly evidenced) | `pins` showed IO13/MTCK reading 1 with the jumper in, 0 without. No IO13 edges appear in the pasted `watch` output; confirm. KEY2 is not part of the test (it reaches IO13 only through SW1, which is OFF); the plan wrongly listed it and is fixed. |
| 2026-09-19 | 4 | 5 | Outputs IO22 and IO19 | pass, one oddity | IO22 followed each `set` immediately (0 and 1 read back correctly); reported working with LED4 and the meter. **IO19: the readback lagged one command** (`set 19 0` printed pad reads 1, `set 19 1` printed pad reads 0). Likely the key-ladder capacitance on that pin, since IO19 is only 1.97 V at idle. It does not matter for nENABLE (a static signal), but STEP must not use IO19. `set` now prints how long the pad takes to follow; re-check IO19 with it. |
| 2026-09-19 | 4 | 6 | Boot-safety check | skipped, risk accepted | Not done. An accidental ring would be audible and easy to patch, and nENABLE has its own external pull-up. Checked again in stage 11 step 2 with the real driver. Keep the boost supply off while wiring. |
| 2026-09-19 | 4 | 5 | IO19 with the new `set` timing | pass | Pad followed each change after 35-38 µs, in both directions. So the earlier lagging readback was capacitance on the pin, not something holding it. Fine for the static nENABLE; **STEP must stay on IO22.** |
| 2026-09-19 | 4 | 4 | Hook jumper 3V3 to MTCK with `watch 13` | pass | Touch gave `-> 1` and release `-> 0` every time. A hand-held jumper chatters: bursts of edges up to about 1.5 ms per touch or release (release chatters more: the 10 kΩ pull-down decays slowly through the threshold). Fast glitches also show as repeated levels, since the ISR reads the pad after the edge. A real switch should bounce less; use software debounce of at least 5 ms (20-50 ms for the hook). |
| 2026-09-19 | 4 | 7 | GPIO budget | decided | Hook IO13/MTCK (active-high), dial pulse IO18, dial-in-progress IO23, STEP IO22, nENABLE IO19 (static only). No board changes; DIP SW3 + SW4 ON. Design docs updated. **Stage 4 complete; the boot-safety check (step 6) was skipped as an accepted risk.** |
| 2026-09-19 | 5 | 1-3 | Wrote the `codec` command (`regs`, `rw`, `init`) in `hwtest/main/cmds_codec.c` | built | Clean build, not yet flashed. The repo's `es8388.c` cannot be reused as is (gCore I2C pins 21/22, `audio_hal` dependency), so its `es8388_init()` register sequence is repeated over our I2C on 33/32 with no internal pull-ups. |
| 2026-09-19 | 5 | 1 | `codec regs` after RESET | pass | All 53 registers (0x00-0x34) read, 0 errors. Values match the ES8388 datasheet reset defaults (0x00=0x06, 0x01=0x5c, 0x02=0xc3, 0x03=0xfc, 0x04=0xc0, 0x08=0x80), so this is a genuine ES8388 in its reset state. Works with no internal I2C pull-ups: the module's own 10 kΩ pull-ups are enough. I2C needs no MCLK. |
| 2026-09-19 | 5 | 2 | `codec rw` | pass | 12 of 12 patterns matched (6 patterns on each of registers 0x1a and 0x1b); originals (0xc0) restored. |
| 2026-09-19 | 5 | 3 | `codec init` | pass | 42 writes, 0 I2C errors, 0 registers read back different. |
| 2026-09-19 | 5 | 4 | `codec regs` after init | pass | As programmed: 0x00=0x12, 0x01=0x50, 0x02=0x00, 0x03=0x09 (ADC and line-in powered), 0x04=0x28 (DAC on, LOUT2/ROUT2), 0x08=0x00 (I2S slave), 0x0a=0x50 (ADC input LINE2), 0x16=0xdb, 0x18=0x02, 0x19=0x64, 0x27/0x2a=0x90, 0x2b=0x80, 0x2e-0x31=0x21. **Stage 5 complete.** |
| 2026-09-19 | 6 | 1-5 | Wrote the audio commands (`audio`, `tone`, `vol`, `volstep`, `mute`) in `hwtest/main/cmds_audio.c` | built | Clean build, not yet flashed. I2S master, MCLK on GPIO0, BCLK 27 / WS 25 / DOUT 26 (older-module map, unconfirmed), APLL, headphone outputs only (DAC power 0x28). Default gain -40 dB. |
| 2026-09-19 | 6 | 1 | `audio on 8000`, `tone 1000`, `vol -20` / `vol -30` | partial | I2S started with MCLK on GPIO0 (APLL 6.144 MHz), codec init clean. **Tone and volume change heard, left channel only.** Both channels carry identical samples, so it is the codec output enable. Hypothesis: DACPOWER (reg 0x04) was 0x28, which by the datasheet bit map (5 LOUT1, 4 ROUT1, 3 LOUT2, 2 ROUT2) enables the left of both outputs; the repo's `DAC_OUTPUT_LOUT2 | ROUT2` constant is 0x28. Fix to test: 0x0c. `codec r/w` added, `audio on` and the init sequence now write 0x0c. Not yet flashed. |
| 2026-09-19 | 6 | 1 | Re-flashed with DAC power 0x0c | pass | **Equal volume in both ears.** Confirms the DACPOWER bit-map fix: the repo's 0x28 for LOUT2/ROUT2 enabled only the left channel. The main firmware's `es8388.c` output constants need the same fix (added to the design doc). |
| 2026-09-19 | 6 | correction | Earpiece path | decided | **The earpiece is on the speaker outputs (J3/J4), not the headphone jack** (user correction). So IO21 is the speaker-amp enable (an output, high = on), LOUT1/ROUT1 need DAC power 0x30, and the amp outputs are bridged (neither pin to ground). Docs updated; `audio out hp\|spk\|both` added to `hwtest` (built, not flashed). Question open: which of J3/J4 and how the earpiece is wired. |
| 2026-09-19 | 6 | 6 | Noise floor | problem | User hears a high noise floor on both the headphones and the speaker outputs, and the speaker has more power than needed. Change: `vol` now attenuates in the analog output stage (registers 0x2e-0x31) first and uses the digital volume only for the remainder, which should lower the codec noise at low levels (built, not yet flashed). Diagnosis steps and board options (resistor pad after the amplifier, lower amp gain via R47-R50) added to the plan, stage 6 step 6. |
| 2026-09-19 | 6 | 6 | Noise diagnosis on the speaker outputs | measured | `mute on`: hiss remains. `vol -80` versus `vol -10`: no change. So the hiss is post-volume (analog output stage or amplifier), not the digital signal. Firmware change: `tone` now defaults to -1 dBFS (was -6) and takes a level, so the amplifier gets the biggest clean signal (built, not flashed). Next: pad or lower the amplifier gain (read the amplifier marking first), and try another power source. |
| 2026-09-19 | 6 | 6 | Amplifier and earpiece identified | recorded | Amplifiers U4 and U5 are marked **NS4150C** ("2548y1"): filterless class-D, 3 W into 4 Ω. The earpiece measures **3.4 Ω DC, marked 4 Ω**. Plan: resistor pad of about 10 Ω in each speaker leg (-15.6 dB), adjust to taste; keep the codec near full scale. |
| 2026-09-19 | 6 | 6 | Resistor pad in each speaker leg | in progress | 33 Ω and 75 Ω each leg (about -25 dB and -32 dB) were still too loud. An earpiece at the ear needs about 1 mW or less against the amplifier's 3 W, so about -40 dB is expected: try 150-330 Ω. Pick R so `vol 4.5` is the loudest level wanted. Record the R chosen and the `vol` for normal listening. |
| 2026-09-20 | 6 | 6 | 820 Ω in each leg | in progress | Works at 40 Hz but 1 kHz is far too loud: the ear and a small earpiece are much less sensitive at 40 Hz, so 40 Hz is the wrong level reference; use 1 kHz. 820 Ω is about -52 dB; try 1.5-4.7 kΩ. To tame the highs added a software filter, `eq lp <Hz>` / `eq hs <Hz> <dB>` (built, not flashed). |
| 2026-09-20 | 6 | 6 | 820 Ω each leg at volstep 1 | pass (noise) | **No audible noise: the hiss problem is solved** (pad plus full-scale codec drive). 60 Hz still audible. A 3300 µF cap was proposed: in series it does nothing (0.03 Hz corner), across the earpiece it would low-pass at 12 Hz. Film caps: 0.33 µF in series = 293 Hz high-pass; 10-22 µF across = 4 to 1.8 kHz low-pass. Added `eq hp <Hz>` to compare in software. |
| 2026-09-20 | 6 | 6 | Pad decision | decided | Keep **820 Ω in each speaker leg**. No audible noise. Slightly loud but fine for now. Frequency tuning (`eq`, film caps) deferred until Bluetooth audio is playing. Remaining in stage 6: amps-off check, sample rates, volume-step table, pops. |
| 2026-09-20 | 6 | 3 | Amps off at reset | pass | Confirmed: RESET (several times) makes no noise, no pop, no sound of any kind from the speaker. |
| 2026-09-20 | - | note | Sample prompt `camilla_montgomery.mp3` analysed | recorded | MP3 mono 44.1 kHz 128 kbit/s, 3.37 s; 17 KB of the 71 KB file is a C2PA metadata tag; nothing above 16 kHz, 99% of energy below 7.4 kHz; peak -10.2 dBFS, RMS -30 dBFS. Proposed format: 16 kHz mono 16-bit WAV, normalized (+9.2 dB here), trimmed. Details in `audio_clips.md`. |
| 2026-09-20 | - | note | Prompt loudness | decided | Normalize prompts in Audacity to -23 LUFS (single-channel, dual-mono off), hard limit -1 dBFS, export 16 kHz mono 16-bit WAV. Procedure and cautions (short clips) in `audio_clips.md`. |
| 2026-09-20 | - | note | One large WAV for many clips | proposed | A bank (`prompts.wav` plus `prompts.idx`, sample offsets from Audacity labels) is possible and fits the Audacity workflow. To be measured in stage 8 step 6 against per-clip files, with and without `CONFIG_FATFS_USE_FASTSEEK` (available in ESP-IDF 4.4.4). Details in `audio_clips.md`. |
| 2026-09-20 | - | note | Single-file clip bank | documented | Notes added to `audio_clips.md` (bank + index format, and how to get the clip boundary metadata from Audacity labels), `initial_design.md` and the stage 8 plan. Per-clip files remain the fallback. |
| 2026-09-20 | 6 | 6 | Frequency response | skipped | Level differences between tones cannot be judged by ear; skipped. Revisit with real Bluetooth audio (stage 9). |
| 2026-09-20 | - | note | Two clip banks (8 kHz and 16 kHz) | documented | Choose the bank by the codec's current I2S rate (the existing firmware knows the call codec via the HFP audio-state event: CVSD 8 kHz, mSBC 16 kHz). Both banks from the same labels, per-bank index. In `audio_clips.md`. |
| 2026-09-20 | 6 | 3, 7 | RESET and noise | pass | No pops on RESET. The noise-floor hiss is gone with the 820 Ω resistors. |
| 2026-09-20 | 6 | 4 | Sample rates 16, 22.05, 44.1 kHz at 1 kHz | pass | No audible quality difference at 1 kHz between the rates, so nothing above 16 kHz is needed. (Pops when changing output or turning audio off and on were not reported.) |
| 2026-09-20 | 6 | 5 | Volume steps | pass | The 10 steps work well on the earpiece; no further evaluation needed. Range stays at the `gain.h` limits (-43.5 to +4.5 dB). |
| 2026-09-20 | 6 | - | **Stage 6 complete** | done | Frequency response skipped. Design decision: **both 8 kHz and 16 kHz clip banks**, because active notifications such as "battery low" can play during a call. Pad: 820 Ω in each speaker leg. |
| 2026-09-20 | 7 | note | MAX9814 wiring documented | recorded | Wiring table added to `initial_design.md` (Microphone). **Open item: the handset cord.** With the earpiece on the bridged speaker outputs, the cord needs five conductors (VCC, GND, mic OUT, earpiece +, earpiece −); the design still says a 3-conductor cord. Decide before the stage 7 cord test. |
| 2026-09-20 | 7 | note | Jacks and cord | recorded | Schematic: J1 (line-in) and J2 (headphone) are both 3.5 mm stereo TRS jacks (6-pin, with switch contacts), no separate mic jack. MAX9814 goes to the line-in tip and sleeve. An 8-conductor handset cord has been ordered (5 needed: mic VCC, GND, OUT, earpiece +, −). Cord pinout to be checked with a meter. |
| 2026-09-20 | 7 | note | Input-path design and firmware | built, not flashed | The onboard mics cannot be excluded in firmware (same LIN2/RIN2 inputs): remove C18 and C20. One channel only is done in firmware (right input and ADC powered down, left ADC to both slots, PGA 0 dB) and must be proved by the stage 7 test. New `mic` commands in `hwtest`. 8-conductor cord: separate power and signal grounds. SNR precautions listed in the plan. |
| 2026-09-20 | 7 | decision | Mic isolation, cord and filtering | decided | Isolate the onboard mics by removing the microphone modules (C18 and C20 are too small); verify with the step 3 test. Cord is 8 straight wires: 1 VCC, 2 and 8 power GND, 3 mic OUT, 4 and 5 signal GND, 6 earpiece +, 7 earpiece −. No twisting and **no software filtering** on the microphone path; hardware only (module supply filter, low MAX9814 gain, parallel grounds, optional series capacitor). |
| 2026-09-20 | 7 | note | MAX9814 gain choice | to test | Question raised: why not 60 dB for better signal-to-noise? Gain does not improve the MAX9814's own signal-to-noise and can clip a close voice or boost room noise in pauses (AGC), so the gain is chosen by measurement: 40, 50 and 60 dB compared on speech RMS minus silence RMS (plan, stage 7 steps 5 and 6). |
| 2026-09-20 | 7 | 1-7 | Wrote the stage 7 test firmware (`mic avg`, `mic snr`, `mic level`, `mic chan`) and rewrote the stage 7 instructions | built, not flashed | Guided tests: noise floor, onboard-mic detection (`NO VOICE DETECTED` after removal), right-channel exclusion with a stereo test tone, MAX9814 gain 40/50/60 dB comparison table, power source, the 8-conductor cord. |
| 2026-09-20 | 7 | note | MAX9814 bench wiring reviewed | recorded | VCC and GAIN to the 3V3 header (GAIN = VCC gives 40 dB), GND to the line-in jack sleeve (AGND), OUT to the line-in tip (left). AR open (1:4000). Fine to start. Improvements: tie GAIN to the module's own VCC pin (after any supply filter), and give the supply current its own return to a header GND so it does not flow through AGND. |
| 2026-09-20 | 7 | note | MAX9814 module-end components | documented | R1 22 Ω in series with VCC, C1 47-100 µF (+ to the filtered node), C2 100 nF, GAIN tied to the filtered VCC node (40 dB), AR open, optional 100-220 Ω in series with OUT. Table added to `initial_design.md`. |
| 2026-09-20 | 7 | decision | MAX9814 module-end parts | decided | R1 22 Ω, C1 100 µF, C2 100 nF on hand. **R2 = 220 Ω in series with OUT: fit it** (isolates the cord capacitance, RF/static protection, about -0.1 dB). Add 3-pin jumper headers for GAIN and AR so stage 7 test 4 needs no rewiring, and a cuttable link in the OUT line for a possible film capacitor later. |
| 2026-09-20 | 7 | note | MAX9814 module vs our added parts | checked against the Adafruit schematic | For the Adafruit 1713 board (EAGLE schematic): ferrite beads in VDD and GND, 2.2 µF across VDD, OUT direct (no R or C), GAIN and A/R unstrapped, no electrolytic. Our R1 22 Ω, C1 100 µF, C2 100 nF, R2 220 Ω and the GAIN/AR headers are not doubled up; supply drop is about 0.07 V at 3 mA. Header order is GND, VDD, GAIN, OUT, A/R (check the silkscreen). To confirm the module is this one: capacitance VDD to GND about 2 µF, GAIN and A/R open. |
| 2026-09-20 | 7 | decision | MAX9814 module identified | assumed | By photo comparison the part count and traces match the Adafruit board; treated as an exact clone. Build sheet (pin table, jumper headers, assembly and checks) added to `initial_design.md`. |
| 2026-09-20 | 7 | correction | MAX9814 GAIN and A/R settings checked against the datasheet | corrected | Datasheet (pin table and Table 1): **GAIN = VDD = 40 dB, GND = 50 dB, open = 60 dB** (confirmed; the module print and Adafruit page agree). **A/R = GND = 1:500, VDD = 1:2000, open = 1:4000**: I had the A/R shunts reversed in the docs; fixed. AGC lowers the gain by at most 20 dB (minimum gain 20/30/40 dB for the three settings). Supply current 3.1 mA typical (6 mA max), output impedance 50 Ω, output bias 1.23 V, maximum output 0.707 Vrms at 1% THD. |
| 2026-09-20 | 7 | note | Wiring diagram | documented | Mermaid diagram of the module end, the 8-wire cord and the board end added to the Microphone section of `initial_design.md`. |
| | 0 | 2, 4, 5 | DIP all OFF, SD empty, battery off, serial driver, UART/POWER port | not reported | |
